import { ProfileDisplay, SearchBar } from ".";

const Navigation = () => (
  <nav>
    <h1>
      Ja<em>mmm</em>ing
    </h1>
    <SearchBar />
    <ProfileDisplay />
  </nav>
);

export default Navigation;
